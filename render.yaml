services:
  - type: web
    name: vegetable-shop-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DATABASE_URL
        value: your_database_url
      - key: JWT_SECRET_KEY
        value: your_secret_key